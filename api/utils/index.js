const deriveBaseURL = require('./deriveBaseURL');
const extractBaseURL = require('./extractBaseURL');
const findMessageContent = require('./findMessageContent');
const applyUseAutumnKey = require('./applyUseAutumnKey');

module.exports = {
  deriveBaseURL,
  extractBaseURL,
  findMessageContent,
  applyUseAutumnKey,
};
