/* eslint-disable */
// prettier-ignore
// ----------------------------------------------------------------------------
//
//  Welcome to Baml! To use this generated code, please run the following:
//
//  $ pnpm add @boundaryml/baml-bridge
//
// ----------------------------------------------------------------------------
// This file was generated by BAML: please do not edit it. Instead, edit the
// BAML files and re-generate this code using: baml-cli generate
// baml-cli is available with the baml package.

import { defineFunction } from "@boundaryml/baml-bridge";

export type json$stream = boolean | number | number | string | json$stream[] | { [key: string]: json$stream } | null;

export type json = boolean | number | number | string | json[] | { [key: string]: json } | null;

/**
 * Thrown when a parsed JSON value does not match the expected BAML type.
 */
export class JsonDecodeError$stream {
  message!: string | null;
  path!: string | null;
  constructor(init: {
    message: string | null;
    path: string | null;
  }) {
    Object.assign(this, init);
  }
}

/**
 * Thrown when a JSON string cannot be parsed (syntax error).
 */
export class JsonParseError$stream {
  message!: string | null;
  constructor(init: {
    message: string | null;
  }) {
    Object.assign(this, init);
  }
}

/**
 * Thrown when a BAML value cannot be serialized to JSON.
 */
export class JsonSerializationError$stream {
  message!: string | null;
  path!: string | null;
  reason!: string | null;
  constructor(init: {
    message: string | null;
    path: string | null;
    reason: string | null;
  }) {
    Object.assign(this, init);
  }
}

/**
 * Thrown when a JSON string cannot be parsed (syntax error).
 */
export class JsonParseError {
  message!: string;
  constructor(init: {
    message: string;
  }) {
    Object.assign(this, init);
  }
}

/**
 * Thrown when a parsed JSON value does not match the expected BAML type.
 */
export class JsonDecodeError {
  message!: string;
  path!: string;
  constructor(init: {
    message: string;
    path: string;
  }) {
    Object.assign(this, init);
  }
}

/**
 * Thrown when a BAML value cannot be serialized to JSON.
 */
export class JsonSerializationError {
  message!: string;
  path!: string;
  reason!: string;
  constructor(init: {
    message: string;
    path: string;
    reason: string;
  }) {
    Object.assign(this, init);
  }
}

/**
 * Parses a JSON string `s` and returns a `json` value. Throws `JsonParseError` on invalid JSON.
 * @throws JsonParseError
 */
export const parse = defineFunction("baml.json.parse", "sync", ["s"]) as (s: string) => json;

/**
 * Parses a JSON string `s` and returns a `json` value. Throws `JsonParseError` on invalid JSON.
 * @throws JsonParseError
 */
export const parse_async = defineFunction("baml.json.parse", "async", ["s"]) as (s: string) => Promise<json>;

/**
 * Serializes a `json` value `j` to a compact JSON string.
 */
export const stringify = defineFunction("baml.json.stringify", "sync", ["j"]) as (j: json) => string;

/**
 * Serializes a `json` value `j` to a compact JSON string.
 */
export const stringify_async = defineFunction("baml.json.stringify", "async", ["j"]) as (j: json) => Promise<string>;

/**
 * Serializes a `json` value `j` to a pretty-printed (indented) JSON string.
 */
export const stringify_pretty = defineFunction("baml.json.stringify_pretty", "sync", ["j"]) as (j: json) => string;

/**
 * Serializes a `json` value `j` to a pretty-printed (indented) JSON string.
 */
export const stringify_pretty_async = defineFunction("baml.json.stringify_pretty", "async", ["j"]) as (j: json) => Promise<string>;

/**
 * @throws JsonSerializationError
 */
export const to_string = defineFunction("baml.json.to_string", "sync", ["v"], undefined, { typeParams: ["T"] }) as <T>(v: T) => string;

/**
 * @throws JsonSerializationError
 */
export const to_string_async = defineFunction("baml.json.to_string", "async", ["v"], undefined, { typeParams: ["T"] }) as <T>(v: T) => Promise<string>;

/**
 * @throws JsonSerializationError
 */
export const to_json = defineFunction("baml.json.to_json", "sync", ["v"], undefined, { typeParams: ["T"] }) as <T>(v: T) => json;

/**
 * @throws JsonSerializationError
 */
export const to_json_async = defineFunction("baml.json.to_json", "async", ["v"], undefined, { typeParams: ["T"] }) as <T>(v: T) => Promise<json>;

/**
 * Serialize any BAML value `value` of type `T` to its `json` representation,
 * honoring user-defined `baml.ToJson` overrides on classes at every depth.
 * 
 * The json analog of `string.from`: a structural conversion that renders
 * primitives, lists, maps, class instances (as `{"field": value}`), enums (as
 * their variant name), and media (as the tagged form) to their natural json
 * shape, routing any value whose runtime class `implements baml.ToJson` through
 * that override. Resolution is on the value's *runtime* class (via the
 * `baml._to_json_shim` native), for the same package-boundary reason
 * `string.from` uses `_to_string_shim` rather than the literal `is baml.ToJson`
 * match form.
 * 
 * Throws `JsonSerializationError` for non-representable values (`uint8array`
 * without explicit encoding, function values, etc.).
 * @throws JsonSerializationError
 */
export const from = defineFunction("baml.json.from", "sync", ["value"], undefined, { typeParams: ["T"] }) as <T>(value: T) => json;

/**
 * Serialize any BAML value `value` of type `T` to its `json` representation,
 * honoring user-defined `baml.ToJson` overrides on classes at every depth.
 * 
 * The json analog of `string.from`: a structural conversion that renders
 * primitives, lists, maps, class instances (as `{"field": value}`), enums (as
 * their variant name), and media (as the tagged form) to their natural json
 * shape, routing any value whose runtime class `implements baml.ToJson` through
 * that override. Resolution is on the value's *runtime* class (via the
 * `baml._to_json_shim` native), for the same package-boundary reason
 * `string.from` uses `_to_string_shim` rather than the literal `is baml.ToJson`
 * match form.
 * 
 * Throws `JsonSerializationError` for non-representable values (`uint8array`
 * without explicit encoding, function values, etc.).
 * @throws JsonSerializationError
 */
export const from_async = defineFunction("baml.json.from", "async", ["value"], undefined, { typeParams: ["T"] }) as <T>(value: T) => Promise<json>;

/**
 * @throws JsonParseError
 * @throws JsonDecodeError
 */
export const from_string = defineFunction("baml.json.from_string", "sync", ["s"], undefined, { typeParams: ["T"] }) as <T>(s: string) => T;

/**
 * @throws JsonParseError
 * @throws JsonDecodeError
 */
export const from_string_async = defineFunction("baml.json.from_string", "async", ["s"], undefined, { typeParams: ["T"] }) as <T>(s: string) => Promise<T>;

/**
 * @throws JsonDecodeError
 */
export const from_json = defineFunction("baml.json.from_json", "sync", ["j"], undefined, { typeParams: ["T"] }) as <T>(j: json) => T;

/**
 * @throws JsonDecodeError
 */
export const from_json_async = defineFunction("baml.json.from_json", "async", ["j"], undefined, { typeParams: ["T"] }) as <T>(j: json) => Promise<T>;

/**
 * @throws JsonDecodeError
 */
export const to = defineFunction("baml.json.to", "sync", ["j"], undefined, { typeParams: ["T"] }) as <T>(j: json) => T;

/**
 * @throws JsonDecodeError
 */
export const to_async = defineFunction("baml.json.to", "async", ["j"], undefined, { typeParams: ["T"] }) as <T>(j: json) => Promise<T>;

export const field = defineFunction("baml.json.field", "sync", ["j", "key"]) as (j: json, key: string) => json;

export const field_async = defineFunction("baml.json.field", "async", ["j", "key"]) as (j: json, key: string) => Promise<json>;

/**
 * Serialize `v` to JSON text, honoring user-defined `to_json` overrides on
 * classes (via [`to_json<T>`]). Equivalent to `stringify(to_json<T>(v))`.
 * 
 * Use this from a host that wants to print a target's return value as JSON
 * while still respecting class-level `to_json` overrides. The structural
 * path is [`to_string<T>`], which walks fields directly and bypasses
 * overrides — appropriate when override behavior would be a problem.
 * 
 * HACK: this is a thin shim that only exists so `baml_exec::dispatch`
 * has a stable named entry point to call from Rust (via
 * `engine.call_function("baml.json.serialize", ...)`). The composition
 * `stringify(to_json<T>(v))` is what callers would write inline anyway;
 * owning a wrapper function gives us one engine-side symbol to look up
 * instead of two, and a place to evolve the override semantics if they
 * drift. Remove this if the host gains a direct way to invoke generic
 * compositions without a named landing pad.
 * @throws JsonSerializationError
 */
export const serialize = defineFunction("baml.json.serialize", "sync", ["v"], undefined, { typeParams: ["T"] }) as <T>(v: T) => string;

/**
 * Serialize `v` to JSON text, honoring user-defined `to_json` overrides on
 * classes (via [`to_json<T>`]). Equivalent to `stringify(to_json<T>(v))`.
 * 
 * Use this from a host that wants to print a target's return value as JSON
 * while still respecting class-level `to_json` overrides. The structural
 * path is [`to_string<T>`], which walks fields directly and bypasses
 * overrides — appropriate when override behavior would be a problem.
 * 
 * HACK: this is a thin shim that only exists so `baml_exec::dispatch`
 * has a stable named entry point to call from Rust (via
 * `engine.call_function("baml.json.serialize", ...)`). The composition
 * `stringify(to_json<T>(v))` is what callers would write inline anyway;
 * owning a wrapper function gives us one engine-side symbol to look up
 * instead of two, and a place to evolve the override semantics if they
 * drift. Remove this if the host gains a direct way to invoke generic
 * compositions without a named landing pad.
 * @throws JsonSerializationError
 */
export const serialize_async = defineFunction("baml.json.serialize", "async", ["v"], undefined, { typeParams: ["T"] }) as <T>(v: T) => Promise<string>;

/**
 * Parse JSON text into a value of type `T`, honoring user-defined
 * `from_json` overrides on classes (via [`from_json<T>`]). Equivalent to
 * `from_json<T>(parse(s))`.
 * 
 * Use this from a host that wants to coerce a JSON-shaped input (such as
 * `--json-args`) into a typed BAML value while respecting class-level
 * `from_json` overrides. The structural path is [`from_string<T>`], which
 * decodes structurally and bypasses overrides.
 * 
 * HACK: same story as [`serialize<T>`] — this wrapper exists so
 * `baml_exec::dispatch::build_args_from_signature` can resolve a single
 * stable name (`baml.json.deserialize`) to coerce `--json-args` payloads
 * through `from_json<T>(parse(s))`. Inlining the composition into the
 * Rust caller would mean threading two generic engine calls + their
 * throws-types instead of one, so the wrapper pays for itself.
 * @throws JsonParseError
 * @throws JsonDecodeError
 */
export const deserialize = defineFunction("baml.json.deserialize", "sync", ["s"], undefined, { typeParams: ["T"] }) as <T>(s: string) => T;

/**
 * Parse JSON text into a value of type `T`, honoring user-defined
 * `from_json` overrides on classes (via [`from_json<T>`]). Equivalent to
 * `from_json<T>(parse(s))`.
 * 
 * Use this from a host that wants to coerce a JSON-shaped input (such as
 * `--json-args`) into a typed BAML value while respecting class-level
 * `from_json` overrides. The structural path is [`from_string<T>`], which
 * decodes structurally and bypasses overrides.
 * 
 * HACK: same story as [`serialize<T>`] — this wrapper exists so
 * `baml_exec::dispatch::build_args_from_signature` can resolve a single
 * stable name (`baml.json.deserialize`) to coerce `--json-args` payloads
 * through `from_json<T>(parse(s))`. Inlining the composition into the
 * Rust caller would mean threading two generic engine calls + their
 * throws-types instead of one, so the wrapper pays for itself.
 * @throws JsonParseError
 * @throws JsonDecodeError
 */
export const deserialize_async = defineFunction("baml.json.deserialize", "async", ["s"], undefined, { typeParams: ["T"] }) as <T>(s: string) => Promise<T>;
