/* eslint-disable */
// prettier-ignore
// ----------------------------------------------------------------------------
//
//  Welcome to Baml! To use this generated code, please run the following:
//
//  $ pnpm add @boundaryml/baml-bridge
//
// ----------------------------------------------------------------------------
// This file was generated by BAML: please do not edit it. Instead, edit the
// BAML files and re-generate this code using: baml-cli generate
// baml-cli is available with the baml package.

import { defineFunction } from "@boundaryml/baml-bridge";
import type * as baml from "../baml/index.js";

export class GetWeather$stream {
  tool!: "get_weather";
  city!: string | null;
  constructor(init: {
    tool: "get_weather";
    city: string | null;
  }) {
    Object.assign(this, init);
  }
}

/**
 * One turn is ONE decision: answer, or call tools. Splitting it into a
 * "select tools" call followed by an "answer" call is what the first version
 * did, and it does not survive contact with a real model — on turn 2 the
 * selector re-picked the tool whose result was already in the transcript, so
 * the loop never terminated. Asking once, with both outcomes in the schema,
 * makes "I already have what I need" an expressible answer rather than a
 * negative instruction the model has to remember to obey.
 * 
 * Also halves the round-trips: one provider call per turn, not two.
 */
export class TurnPlan$stream {
  reply!: string | null;
  tools!: (GetWeather$stream | WebSearch$stream)[];
  constructor(init: {
    reply: string | null;
    tools: (GetWeather$stream | WebSearch$stream)[];
  }) {
    Object.assign(this, init);
  }
}

export class WebSearch$stream {
  tool!: "web_search";
  query!: string | null;
  constructor(init: {
    tool: "web_search";
    query: string | null;
  }) {
    Object.assign(this, init);
  }
}

export class GetWeather {
  tool!: "get_weather";
  city!: string;
  constructor(init: {
    tool: "get_weather";
    city: string;
  }) {
    Object.assign(this, init);
  }
}

export class WebSearch {
  tool!: "web_search";
  query!: string;
  constructor(init: {
    tool: "web_search";
    query: string;
  }) {
    Object.assign(this, init);
  }
}

/**
 * One turn is ONE decision: answer, or call tools. Splitting it into a
 * "select tools" call followed by an "answer" call is what the first version
 * did, and it does not survive contact with a real model — on turn 2 the
 * selector re-picked the tool whose result was already in the transcript, so
 * the loop never terminated. Asking once, with both outcomes in the schema,
 * makes "I already have what I need" an expressible answer rather than a
 * negative instruction the model has to remember to obey.
 * 
 * Also halves the round-trips: one provider call per turn, not two.
 */
export class TurnPlan {
  reply!: string | null;
  tools!: (GetWeather | WebSearch)[];
  constructor(init: {
    reply: string | null;
    tools: (GetWeather | WebSearch)[];
  }) {
    Object.assign(this, init);
  }
}

/**
 * The return type is static, so `${ctx.output_format}` renders a real schema
 * and the live path never reaches the type-var renderer that panics.
 * `tools` being a list-of-union is what allows parallel tool calls in one turn.
 * @throws DevOther
 * @throws InvalidArgument
 * @throws Io
 * @throws LlmClient
 * @throws RenderPrompt
 * @throws Timeout
 */
export const HostTurn = defineFunction("user.host.HostTurn", "sync", ["user_message", "transcript"], ["client"]) as (user_message: string, transcript: string, $opts?: { client?: baml.llm.Client | undefined } | undefined) => TurnPlan;

/**
 * The return type is static, so `${ctx.output_format}` renders a real schema
 * and the live path never reaches the type-var renderer that panics.
 * `tools` being a list-of-union is what allows parallel tool calls in one turn.
 * @throws DevOther
 * @throws InvalidArgument
 * @throws Io
 * @throws LlmClient
 * @throws RenderPrompt
 * @throws Timeout
 */
export const HostTurn_async = defineFunction("user.host.HostTurn", "async", ["user_message", "transcript"], ["client"]) as (user_message: string, transcript: string, $opts?: { client?: baml.llm.Client | undefined } | undefined) => Promise<TurnPlan>;

/**
 * The return type is static, so `${ctx.output_format}` renders a real schema
 * and the live path never reaches the type-var renderer that panics.
 * `tools` being a list-of-union is what allows parallel tool calls in one turn.
 * @throws InvalidArgument
 * @throws LlmClient
 * @throws RenderPrompt
 */
export const HostTurn$build_request = defineFunction("user.host.HostTurn$build_request", "sync", ["user_message", "transcript"], ["client"]) as (user_message: string, transcript: string, $opts?: { client?: baml.llm.Client | undefined } | undefined) => baml.http.Request;

/**
 * The return type is static, so `${ctx.output_format}` renders a real schema
 * and the live path never reaches the type-var renderer that panics.
 * `tools` being a list-of-union is what allows parallel tool calls in one turn.
 * @throws InvalidArgument
 * @throws LlmClient
 * @throws RenderPrompt
 */
export const HostTurn$build_request_async = defineFunction("user.host.HostTurn$build_request", "async", ["user_message", "transcript"], ["client"]) as (user_message: string, transcript: string, $opts?: { client?: baml.llm.Client | undefined } | undefined) => Promise<baml.http.Request>;

/**
 * The return type is static, so `${ctx.output_format}` renders a real schema
 * and the live path never reaches the type-var renderer that panics.
 * `tools` being a list-of-union is what allows parallel tool calls in one turn.
 * @throws InvalidArgument
 * @throws LlmClient
 * @throws RenderPrompt
 */
export const HostTurn$build_request_stream = defineFunction("user.host.HostTurn$build_request_stream", "sync", ["user_message", "transcript"], ["client"]) as (user_message: string, transcript: string, $opts?: { client?: baml.llm.Client | undefined } | undefined) => baml.http.Request;

/**
 * The return type is static, so `${ctx.output_format}` renders a real schema
 * and the live path never reaches the type-var renderer that panics.
 * `tools` being a list-of-union is what allows parallel tool calls in one turn.
 * @throws InvalidArgument
 * @throws LlmClient
 * @throws RenderPrompt
 */
export const HostTurn$build_request_stream_async = defineFunction("user.host.HostTurn$build_request_stream", "async", ["user_message", "transcript"], ["client"]) as (user_message: string, transcript: string, $opts?: { client?: baml.llm.Client | undefined } | undefined) => Promise<baml.http.Request>;

/**
 * The return type is static, so `${ctx.output_format}` renders a real schema
 * and the live path never reaches the type-var renderer that panics.
 * `tools` being a list-of-union is what allows parallel tool calls in one turn.
 * @throws ParseError
 */
export const HostTurn$parse = defineFunction("user.host.HostTurn$parse", "sync", ["json"], ["client"]) as (json: string, $opts?: { client?: baml.llm.Client | undefined } | undefined) => TurnPlan;

/**
 * The return type is static, so `${ctx.output_format}` renders a real schema
 * and the live path never reaches the type-var renderer that panics.
 * `tools` being a list-of-union is what allows parallel tool calls in one turn.
 * @throws ParseError
 */
export const HostTurn$parse_async = defineFunction("user.host.HostTurn$parse", "async", ["json"], ["client"]) as (json: string, $opts?: { client?: baml.llm.Client | undefined } | undefined) => Promise<TurnPlan>;

/**
 * The return type is static, so `${ctx.output_format}` renders a real schema
 * and the live path never reaches the type-var renderer that panics.
 * `tools` being a list-of-union is what allows parallel tool calls in one turn.
 * @throws InvalidArgument
 * @throws LlmClient
 */
export const HostTurn$parse_stream = defineFunction("user.host.HostTurn$parse_stream", "sync", ["sse"], ["client"]) as (sse: baml.http.SseStream, $opts?: { client?: baml.llm.Client | undefined } | undefined) => baml.llm.Stream<TurnPlan$stream | null, TurnPlan>;

/**
 * The return type is static, so `${ctx.output_format}` renders a real schema
 * and the live path never reaches the type-var renderer that panics.
 * `tools` being a list-of-union is what allows parallel tool calls in one turn.
 * @throws InvalidArgument
 * @throws LlmClient
 */
export const HostTurn$parse_stream_async = defineFunction("user.host.HostTurn$parse_stream", "async", ["sse"], ["client"]) as (sse: baml.http.SseStream, $opts?: { client?: baml.llm.Client | undefined } | undefined) => Promise<baml.llm.Stream<TurnPlan$stream | null, TurnPlan>>;

/**
 * The return type is static, so `${ctx.output_format}` renders a real schema
 * and the live path never reaches the type-var renderer that panics.
 * `tools` being a list-of-union is what allows parallel tool calls in one turn.
 * @throws InvalidArgument
 * @throws LlmClient
 * @throws RenderPrompt
 */
export const HostTurn$render_prompt = defineFunction("user.host.HostTurn$render_prompt", "sync", ["user_message", "transcript"], ["client"]) as (user_message: string, transcript: string, $opts?: { client?: baml.llm.Client | undefined } | undefined) => baml.llm.PromptAst;

/**
 * The return type is static, so `${ctx.output_format}` renders a real schema
 * and the live path never reaches the type-var renderer that panics.
 * `tools` being a list-of-union is what allows parallel tool calls in one turn.
 * @throws InvalidArgument
 * @throws LlmClient
 * @throws RenderPrompt
 */
export const HostTurn$render_prompt_async = defineFunction("user.host.HostTurn$render_prompt", "async", ["user_message", "transcript"], ["client"]) as (user_message: string, transcript: string, $opts?: { client?: baml.llm.Client | undefined } | undefined) => Promise<baml.llm.PromptAst>;

/**
 * The return type is static, so `${ctx.output_format}` renders a real schema
 * and the live path never reaches the type-var renderer that panics.
 * `tools` being a list-of-union is what allows parallel tool calls in one turn.
 * @throws DevOther
 * @throws InvalidArgument
 * @throws Io
 * @throws LlmClient
 * @throws RenderPrompt
 * @throws Timeout
 */
export const HostTurn$stream = defineFunction("user.host.HostTurn$stream", "sync", ["user_message", "transcript"], ["client"]) as (user_message: string, transcript: string, $opts?: { client?: baml.llm.Client | undefined } | undefined) => baml.llm.Stream<TurnPlan$stream | null, TurnPlan>;

/**
 * The return type is static, so `${ctx.output_format}` renders a real schema
 * and the live path never reaches the type-var renderer that panics.
 * `tools` being a list-of-union is what allows parallel tool calls in one turn.
 * @throws DevOther
 * @throws InvalidArgument
 * @throws Io
 * @throws LlmClient
 * @throws RenderPrompt
 * @throws Timeout
 */
export const HostTurn$stream_async = defineFunction("user.host.HostTurn$stream", "async", ["user_message", "transcript"], ["client"]) as (user_message: string, transcript: string, $opts?: { client?: baml.llm.Client | undefined } | undefined) => Promise<baml.llm.Stream<TurnPlan$stream | null, TurnPlan>>;

/**
 * Bridge-safe transcript: parallel primitive arrays only. An array of class
 * instances lowers to maps and panics, so the host flattens before crossing.
 */
export const host_transcript = defineFunction("user.host.host_transcript", "sync", ["names", "args", "results"]) as (names: string[], args: string[], results: string[]) => string;

/**
 * Bridge-safe transcript: parallel primitive arrays only. An array of class
 * instances lowers to maps and panics, so the host flattens before crossing.
 */
export const host_transcript_async = defineFunction("user.host.host_transcript", "async", ["names", "args", "results"]) as (names: string[], args: string[], results: string[]) => Promise<string>;
