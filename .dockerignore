**/.circleci
**/.editorconfig
**/.dockerignore
**/.git
**/.DS_Store
**/.vscode
**/node_modules

# Specific patterns to ignore
data-node
meili_data*
# Anchored to root so this only matches librechat.yaml/librechat.example.yaml
# and not similarly-named paths elsewhere in the tree (unanchored `librechat*`
# would match at any depth).
/librechat*.yaml
Dockerfile*
docs

# Ignore all hidden files
.*
