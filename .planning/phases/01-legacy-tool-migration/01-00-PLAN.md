---
phase: 01-legacy-tool-migration
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - client/src/components/Chat/Messages/Content/__tests__/ImageGen.test.tsx
  - client/src/components/Chat/Messages/Content/__tests__/RetrievalCall.test.tsx
autonomous: true
requirements:
  - LGCY-01
  - LGCY-02
  - LGCY-03

must_haves:
  truths:
    - "Test stubs exist and run (even if they fail) before any implementation begins"
    - "ImageGen test covers legacy props path, agent-style props path, and localized text"
    - "RetrievalCall test covers ToolIcon rendering, collapsible panel, and localized text"
  artifacts:
    - path: "client/src/components/Chat/Messages/Content/__tests__/ImageGen.test.tsx"
      provides: "Test stubs for merged ImageGen component (LGCY-01, LGCY-03)"
      min_lines: 60
    - path: "client/src/components/Chat/Messages/Content/__tests__/RetrievalCall.test.tsx"
      provides: "Test stubs for modernized RetrievalCall component (LGCY-02, LGCY-03)"
      min_lines: 50
  key_links:
    - from: "client/src/components/Chat/Messages/Content/__tests__/ImageGen.test.tsx"
      to: "Parts/OpenAIImageGen/OpenAIImageGen.tsx"
      via: "import of merged ImageGen component"
      pattern: "import.*ImageGen"
    - from: "client/src/components/Chat/Messages/Content/__tests__/RetrievalCall.test.tsx"
      to: "RetrievalCall.tsx"
      via: "import of modernized RetrievalCall component"
      pattern: "import.*RetrievalCall"
---

<objective>
Create test stub files for ImageGen and RetrievalCall BEFORE implementation begins. These stubs define the expected behaviors from LGCY-01, LGCY-02, and LGCY-03, providing the Nyquist-compliant verification targets that implementation tasks run against.

Purpose: Wave 0 establishes behavioral verification targets so that Plan 01 and Plan 02 verify blocks run actual tests, not just type checks.
Output: Two test files with failing tests that describe expected post-migration behaviors.
</objective>

<execution_context>
@/home/berry13/.claude/get-shit-done/workflows/execute-plan.md
@/home/berry13/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-legacy-tool-migration/01-CONTEXT.md
@.planning/phases/01-legacy-tool-migration/01-RESEARCH.md

@client/src/components/Chat/Messages/Content/__tests__/ToolCall.test.tsx
@client/src/components/Chat/Messages/Content/Parts/OpenAIImageGen/OpenAIImageGen.tsx
@client/src/components/Chat/Messages/Content/Parts/OpenAIImageGen/ProgressText.tsx
@client/src/components/Chat/Messages/Content/RetrievalCall.tsx
@client/src/components/Chat/Messages/Content/ToolOutput/ToolIcon.tsx
@client/src/components/Chat/Messages/Content/ProgressText.tsx

<interfaces>
<!-- ToolCall.test.tsx serves as the mock pattern template. Reuse the same mock structure. -->

From ToolCall.test.tsx mock pattern:
```typescript
jest.mock('~/hooks', () => ({
  useLocalize: () => (key: string, values?: any) => {
    const translations: Record<string, string> = { /* key -> value */ };
    return translations[key] || key;
  },
  useProgress: (initialProgress: number) => (initialProgress >= 1 ? 1 : initialProgress),
}));
```

From OpenAIImageGen.tsx current props:
```typescript
{
  initialProgress: number;
  isSubmitting: boolean;
  toolName: string;
  args: string | Record<string, unknown>;
  output?: string | null;
  attachments?: TAttachment[];
}
```

From RetrievalCall.tsx current props:
```typescript
{
  initialProgress: number;
  isSubmitting: boolean;
}
```

Post-migration RetrievalCall will accept:
```typescript
{
  initialProgress: number;
  isSubmitting: boolean;
  output?: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ImageGen test stubs</name>
  <files>client/src/components/Chat/Messages/Content/__tests__/ImageGen.test.tsx</files>
  <action>
    Create test file following the ToolCall.test.tsx pattern (same mock structure, RecoilRoot wrapping).

    **Mock setup (copy pattern from ToolCall.test.tsx):**
    - Mock `~/hooks` with `useLocalize` returning translations for new keys: `com_ui_generating_image` ("Generating image..."), `com_ui_image_created` ("Image created"), `com_ui_image_gen_failed` ("Image generation failed"). Also mock `useProgress` the same way ToolCall.test.tsx does.
    - Mock `./ProgressText` (the OpenAIImageGen-specific ProgressText) as a simple div that renders `toolName`, progress text based on error/progress state. Keep it simple -- just render the props so tests can assert on them.
    - Mock `@librechat/client` to stub `PixelCard` as a div with data-testid="pixel-card".
    - Mock `~/components/Chat/Messages/Content/Image` as a div with data-testid="image".
    - Mock `../ToolOutput` to stub `ToolIcon` as `({ type, isAnimating }) => <span data-testid="tool-icon" data-type={type} data-animating={isAnimating} />` and `isError` as `(output) => typeof output === 'string' && output.toLowerCase().includes('error')`.
    - Mock `~/utils` with `scaleImage` returning `{ width: '512px', height: '512px' }` and `logger` with `{ error: jest.fn(), debug: jest.fn() }` and `cn` joining classes.

    **Import:** Import from `../Parts/OpenAIImageGen/OpenAIImageGen` (or `../Parts` once barrel is updated -- use the direct path since barrel may not export `ImageGen` yet at Wave 0). Since the component name is `OpenAIImageGen` now but will become `ImageGen` after Plan 01, import the default export and alias it: `import ImageGen from '../Parts/OpenAIImageGen/OpenAIImageGen';`

    **Test describe blocks with stubs:**

    1. `describe('ImageGen - LGCY-01: Modern visual patterns')`:
       - `it('renders ToolIcon with type="image_gen" for agent-style tools')` -- render with agent-style props (`toolName: 'image_gen_oai'`, `isSubmitting: false`, `initialProgress: 1`, `args: '{}'`, `output: ''`), assert `data-testid="tool-icon"` is present with `data-type="image_gen"`.
       - `it('renders ToolIcon with type="image_gen" for legacy tools')` -- render with legacy props (`initialProgress: 1`, `args: '{"prompt":"test"}'`, NO `toolName`), assert ToolIcon is present. Note: this test will FAIL until Plan 01 adds ToolIcon to the component.
       - `it('does not render ProgressCircle')` -- render component, assert no element with ProgressCircle class or testid.
       - `it('renders PixelCard during agent-style image generation')` -- render with `isSubmitting: true`, `initialProgress: 0.5`, `toolName: 'image_gen_oai'`, `args: '{"size":"1024x1024"}'`, assert `data-testid="pixel-card"` is present.

    2. `describe('ImageGen - LGCY-01: Legacy and agent-style unification')`:
       - `it('accepts legacy props (initialProgress + args only)')` -- render with just `initialProgress: 1` and `args: '{"prompt":"a cat"}'`, assert it renders without crashing. Note: this test will FAIL until Plan 01 makes `isSubmitting`/`toolName`/`output`/`attachments` optional.
       - `it('accepts agent-style props (full set)')` -- render with all props, assert it renders without crashing.
       - `it('handles history reload state (initialProgress=1, no isSubmitting)')` -- render with `initialProgress: 1`, no `isSubmitting`, assert progress text shows completed state.

    3. `describe('ImageGen - LGCY-03: Localization')`:
       - `it('does not contain hardcoded "Creating Image" text')` -- render with `isSubmitting: true`, `initialProgress: 0.5`, `args: '{}'`, assert the rendered output does NOT contain the literal text "Creating Image".
       - `it('does not contain hardcoded "Finished." text')` -- render completed state, assert no "Finished." text.

    **Wrapper helper:** `const renderImageGen = (props) => render(<RecoilRoot><ImageGen {...defaultProps} {...props} /></RecoilRoot>);`

    **Default props:** `{ initialProgress: 1, isSubmitting: false, toolName: 'image_gen_oai', args: '{}', output: '' }`

    These tests define the behavioral contract. Some will FAIL against the current code (expected -- they verify post-migration behavior). The implementation in Plan 01 makes them pass.
  </action>
  <verify>
    <automated>cd /home/berry13/librechat/client && npx jest --testPathPattern="__tests__/ImageGen" --no-coverage 2>&1 | tail -20</automated>
  </verify>
  <done>
    - ImageGen.test.tsx exists with tests covering LGCY-01 (ToolIcon, no ProgressCircle, legacy+agent unification) and LGCY-03 (no hardcoded strings)
    - Tests run without import/syntax errors (some may fail -- expected for Wave 0 stubs)
    - Mock structure follows ToolCall.test.tsx pattern
  </done>
</task>

<task type="auto">
  <name>Task 2: Create RetrievalCall test stubs</name>
  <files>client/src/components/Chat/Messages/Content/__tests__/RetrievalCall.test.tsx</files>
  <action>
    Create test file following the ToolCall.test.tsx pattern.

    **Mock setup:**
    - Mock `~/hooks` with `useLocalize` returning translations for: `com_ui_searching_files` ("Searching files..."), `com_ui_retrieved_files` ("Retrieved files"), `com_ui_retrieval_failed` ("failed"), `com_ui_tool_failed` ("failed"). Also mock `useProgress` same as ToolCall.test.tsx.
    - Mock `./ProgressText` (shared Content/ProgressText.tsx) as: `({ onClick, inProgressText, finishedText, icon, hasInput, isExpanded, error, errorSuffix }) => <div onClick={onClick} data-testid="progress-text" data-in-progress={inProgressText} data-finished={finishedText} data-has-input={hasInput} data-expanded={isExpanded} data-error={error} data-error-suffix={errorSuffix}>{finishedText || inProgressText}</div>`
    - Mock `./ToolOutput` with `ToolIcon` as `({ type, isAnimating }) => <span data-testid="tool-icon" data-type={type} data-animating={isAnimating} />` and `isError` as `(output) => typeof output === 'string' && output.toLowerCase().includes('error')`.
    - Mock `~/hooks/Messages/useExpandCollapse` as `(isExpanded) => ({ expandStyle: isExpanded ? {} : { display: 'none' }, isTransitioning: false })`.
    - Mock `./ProgressCircle`, `./InProgressCall`, `./RetrievalIcon`, `./CancelledIcon` as simple divs with testids (so tests can assert they're NOT rendered after migration).
    - Mock `~/utils` with `cn` and `logger`.

    **Import:** `import RetrievalCall from '../RetrievalCall';`

    **Test describe blocks:**

    1. `describe('RetrievalCall - LGCY-02: Modern visual patterns')`:
       - `it('renders ToolIcon with type="file_search"')` -- render with `initialProgress: 1`, `isSubmitting: false`, assert `data-testid="tool-icon"` has `data-type="file_search"`.
       - `it('does not render ProgressCircle')` -- render, assert `data-testid="progress-circle"` is NOT in the document.
       - `it('does not render InProgressCall')` -- render, assert `data-testid="in-progress-call"` is NOT in the document.
       - `it('does not render RetrievalIcon')` -- render, assert `data-testid="retrieval-icon"` is NOT in the document.

    2. `describe('RetrievalCall - LGCY-02: Collapsible output panel')`:
       - `it('shows collapsible panel when output exists and is clicked')` -- render with `output: 'file results here'`, `initialProgress: 1`, `isSubmitting: false`, click on progress text, assert output text is visible.
       - `it('does not show panel when output is undefined')` -- render without output prop, assert no output panel content visible.
       - `it('does not show panel when output contains error')` -- render with `output: 'error processing tool'`, assert panel is not shown (isError returns true so hasOutput is false).

    3. `describe('RetrievalCall - LGCY-03: Localization')`:
       - `it('uses localized in-progress text')` -- render with `initialProgress: 0.5`, `isSubmitting: true`, assert progress text contains "Searching files..." (from mock localize).
       - `it('uses localized finished text')` -- render with `initialProgress: 1`, `isSubmitting: false`, assert progress text contains "Retrieved files".
       - `it('does not contain hardcoded "Searching my knowledge" text')` -- render in-progress state, assert text does NOT contain "Searching my knowledge".
       - `it('does not contain hardcoded "Used Retrieval" text')` -- render completed state, assert text does NOT contain "Used Retrieval".

    **Wrapper:** `const renderRetrievalCall = (props) => render(<RecoilRoot><RetrievalCall {...defaultProps} {...props} /></RecoilRoot>);`

    **Default props:** `{ initialProgress: 1, isSubmitting: false }`

    These tests verify post-migration behavior. Tests checking for absence of deprecated components and hardcoded strings will FAIL against current code (expected). Plan 02 makes them pass.
  </action>
  <verify>
    <automated>cd /home/berry13/librechat/client && npx jest --testPathPattern="__tests__/RetrievalCall" --no-coverage 2>&1 | tail -20</automated>
  </verify>
  <done>
    - RetrievalCall.test.tsx exists with tests covering LGCY-02 (ToolIcon, no deprecated components, collapsible panel) and LGCY-03 (localized text, no hardcoded strings)
    - Tests run without import/syntax errors (some will fail -- expected for Wave 0 stubs)
    - Mock structure follows ToolCall.test.tsx pattern
  </done>
</task>

</tasks>

<verification>
1. `cd /home/berry13/librechat/client && npx jest --testPathPattern="__tests__/(ImageGen|RetrievalCall)" --no-coverage 2>&1 | tail -30` -- Both test files run (failures expected at Wave 0)
2. `ls client/src/components/Chat/Messages/Content/__tests__/ImageGen.test.tsx client/src/components/Chat/Messages/Content/__tests__/RetrievalCall.test.tsx` -- Both files exist
</verification>

<success_criteria>
- Both test stub files exist and are syntactically valid (Jest can parse and run them)
- ImageGen tests cover: ToolIcon rendering, no ProgressCircle, legacy+agent prop paths, no hardcoded strings
- RetrievalCall tests cover: ToolIcon rendering, no deprecated components, collapsible panel, localized text
- Test failures at Wave 0 are expected -- they define the target behavior for Plans 01 and 02
</success_criteria>

<output>
After completion, create `.planning/phases/01-legacy-tool-migration/01-00-SUMMARY.md`
</output>
