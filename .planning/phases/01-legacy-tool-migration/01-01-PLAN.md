---
phase: 01-legacy-tool-migration
plan: 01
type: execute
wave: 1
depends_on:
  - 01-00
files_modified:
  - client/src/components/Chat/Messages/Content/Parts/OpenAIImageGen/OpenAIImageGen.tsx
  - client/src/components/Chat/Messages/Content/Parts/OpenAIImageGen/ProgressText.tsx
  - client/src/components/Chat/Messages/Content/Parts/OpenAIImageGen/index.ts
  - client/src/components/Chat/Messages/Content/Parts/index.ts
  - client/src/components/Chat/Messages/Content/ImageGen.tsx
  - client/src/components/Chat/Messages/Content/Part.tsx
  - client/src/locales/en/translation.json
autonomous: true
requirements:
  - LGCY-01
  - LGCY-03

must_haves:
  truths:
    - "Legacy function-type image gen tools (dalle, stable-diffusion, flux) render with ToolIcon and localized ProgressText, not inline SVG and hardcoded English"
    - "Agent-style image gen tools (image_gen_oai, image_edit_oai, gemini_image_gen) continue to render with PixelCard and quality-aware staged progress text"
    - "Both legacy and agent-style image gen paths use the same merged ImageGen component"
    - "ImageGen has NO collapsible output panel"
    - "No hardcoded English strings remain in ImageGen or its ProgressText"
    - "ImageGen works in all three render contexts: streaming, completed message, history reload"
  artifacts:
    - path: "client/src/components/Chat/Messages/Content/Parts/OpenAIImageGen/OpenAIImageGen.tsx"
      provides: "Merged ImageGen component handling both legacy and agent-style paths"
      min_lines: 80
    - path: "client/src/components/Chat/Messages/Content/Parts/OpenAIImageGen/ProgressText.tsx"
      provides: "Domain-specific progress text with localized strings for all image gen tool types"
      min_lines: 30
    - path: "client/src/components/Chat/Messages/Content/Part.tsx"
      provides: "Updated dispatch routing both image gen paths to merged component"
  key_links:
    - from: "client/src/components/Chat/Messages/Content/Part.tsx"
      to: "OpenAIImageGen (merged ImageGen)"
      via: "Both ToolCallTypes.FUNCTION+imageGenTools and agent-style name checks route to same component"
      pattern: "ImageGen|OpenAIImageGen"
    - from: "client/src/components/Chat/Messages/Content/Parts/OpenAIImageGen/OpenAIImageGen.tsx"
      to: "ToolIcon"
      via: "import from ToolOutput"
      pattern: "ToolIcon.*image_gen"
    - from: "client/src/components/Chat/Messages/Content/Parts/OpenAIImageGen/ProgressText.tsx"
      to: "useLocalize"
      via: "All text strings use localization hook"
      pattern: "useLocalize|localize\\("
---

<objective>
Merge the old ImageGen component (inline SVG, ProgressCircle, hardcoded English) into OpenAIImageGen to create a single unified ImageGen component. The merged component handles both legacy function-type image gen tools (dalle, stable-diffusion, flux) and agent-style tools (image_gen_oai, image_edit_oai, gemini_image_gen). Update Part.tsx so both dispatch paths route to the merged component.

Purpose: LGCY-01 requires ImageGen to use modern visual patterns (ToolIcon, semantic tokens). LGCY-03 requires all hardcoded English strings to be localized.
Output: A single ImageGen component with no inline SVGs, no hardcoded colors, no hardcoded English text. Old ImageGen.tsx becomes a thin re-export.
</objective>

<execution_context>
@/home/berry13/.claude/get-shit-done/workflows/execute-plan.md
@/home/berry13/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-legacy-tool-migration/01-CONTEXT.md
@.planning/phases/01-legacy-tool-migration/01-RESEARCH.md
@.planning/phases/01-legacy-tool-migration/01-00-SUMMARY.md

@client/src/components/Chat/Messages/Content/ToolCall.tsx
@client/src/components/Chat/Messages/Content/Parts/OpenAIImageGen/OpenAIImageGen.tsx
@client/src/components/Chat/Messages/Content/Parts/OpenAIImageGen/ProgressText.tsx
@client/src/components/Chat/Messages/Content/ImageGen.tsx
@client/src/components/Chat/Messages/Content/Part.tsx
@client/src/components/Chat/Messages/Content/Parts/index.ts
@client/src/components/Chat/Messages/Content/Parts/OpenAIImageGen/index.ts
@client/src/components/Chat/Messages/Content/ToolOutput/ToolIcon.tsx
@client/src/components/Chat/Messages/Content/__tests__/ImageGen.test.tsx

<interfaces>
<!-- Key types and contracts the executor needs. -->

From ToolIcon.tsx:
```typescript
export type ToolIconType =
  | 'mcp' | 'execute_code' | 'web_search'
  | 'image_gen' | 'agent_handoff' | 'file_search' | 'generic';
// Props: { type: ToolIconType; iconUrl?: string; isAnimating?: boolean }
```

From shared ProgressText.tsx (Content/ProgressText.tsx):
```typescript
// Props interface:
{
  progress: number;
  onClick?: () => void;
  inProgressText: string;
  finishedText: string;
  authText?: string;
  icon?: React.ReactNode;
  subtitle?: string;
  errorSuffix?: string;
  hasInput?: boolean;
  popover?: boolean;
  isExpanded?: boolean;
  error?: boolean;
}
```

From Part.tsx dispatch - agent-style image gen props:
```typescript
// Line 131-141: agent-style tools pass these props:
{ initialProgress, isSubmitting, toolName: name, args: string, output: string, attachments }
```

From Part.tsx dispatch - legacy function-type image gen props:
```typescript
// Line 192-193: legacy tools pass these props:
{ initialProgress, args: fn.arguments as string }
// NOTE: no isSubmitting, no output, no attachments, no toolName
```

From data-provider:
```typescript
// imageGenTools: Set<string> containing 'dalle', 'stable-diffusion', 'flux', etc.
// ToolCallTypes.FUNCTION = 'function'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Merge ImageGen components and update exports</name>
  <files>
    client/src/components/Chat/Messages/Content/Parts/OpenAIImageGen/OpenAIImageGen.tsx
    client/src/components/Chat/Messages/Content/Parts/OpenAIImageGen/ProgressText.tsx
    client/src/components/Chat/Messages/Content/Parts/OpenAIImageGen/index.ts
    client/src/components/Chat/Messages/Content/Parts/index.ts
    client/src/components/Chat/Messages/Content/ImageGen.tsx
    client/src/locales/en/translation.json
  </files>
  <action>
    **Modify OpenAIImageGen.tsx to handle both legacy and agent-style paths:**

    1. Make `isSubmitting`, `toolName`, `output`, and `attachments` props optional with sensible defaults. Legacy function-type callers pass only `initialProgress` and `args`. When `isSubmitting` is undefined and `initialProgress >= 1`, treat as completed (history/completed context). When `isSubmitting` is undefined and `initialProgress < 1`, treat as in-progress.

    2. Add a `ToolIcon` import from `../ToolOutput` (or `./ToolOutput` depending on relative path -- use `'../../ToolOutput'` since OpenAIImageGen is in Parts/OpenAIImageGen/). Render `<ToolIcon type="image_gen" isAnimating={isInProgress} />` alongside the ProgressText. Currently the component renders ProgressText in a bare `div` with no icon -- add the ToolIcon as the `icon` prop to ProgressText or as a sibling element, matching how ToolCall.tsx does it. The current OpenAIImageGen/ProgressText is a domain-specific component (NOT the shared Content/ProgressText.tsx), so the ToolIcon should be rendered as a sibling in the parent, positioned identically to how ToolCall.tsx does it: a flex container with the icon and progress text.

    3. For the progress logic:
       - When `toolName` is one of 'image_gen_oai', 'image_edit_oai', 'gemini_image_gen' (agent-style): keep the existing quality-aware custom `setInterval` timing logic per user decision.
       - When `toolName` is absent or not in that set (legacy function-type): use `useProgress(initialProgress)` hook from `~/hooks` for standard progress animation. This means the component needs conditional progress logic based on `toolName`.

    4. Remove the `console.error` on line 41 (replace with silent fallback or use `logger.debug` from `~/utils` if needed).

    5. PixelCard stays as-is per user decision -- no container chrome wrapper around it.

    6. No collapsible output panel per user decision -- the image is the output.

    7. Error detection: currently checks `output.toLowerCase().includes('error processing tool')`. Replace with `isError(output)` from `'../../ToolOutput'` for consistent error detection. Import `isError` alongside `ToolIcon`.

    8. No subtitle on ImageGen per user decision (no "via DALL-E" or model name).

    **Modify ProgressText.tsx (OpenAIImageGen-specific):**

    1. Add a fallback path for legacy tools (when `toolName` is not one of the three agent-style names). Use localized strings:
       - In-progress: add a new key `com_ui_generating_image` = "Generating image..." to translation.json
       - Completed: use existing `com_ui_image_created` = "Image created"
       - Error: use existing `com_ui_error`
    These are domain-specific texts per user decision (NOT generic "Running {name}" / "Completed {name}").

    2. Domain-specific error text: for all image gen paths, use `localize('com_ui_image_gen_failed')` instead of generic error text. Add new key `com_ui_image_gen_failed` = "Image generation failed" to translation.json.

    **Update OpenAIImageGen/index.ts:**
    - Add a named export alias: `export { default as ImageGen } from './OpenAIImageGen';`
    - Keep the existing `OpenAIImageGen` export for backward compatibility during transition.

    **Update Parts/index.ts:**
    - Add `ImageGen` to the re-export from `'./OpenAIImageGen'`: the line `export * from './OpenAIImageGen'` already re-exports everything, so once the named export exists in OpenAIImageGen/index.ts, it will be available.

    **Replace old ImageGen.tsx:**
    - Replace the entire file content with a thin re-export: `export { ImageGen as default } from './Parts/OpenAIImageGen';`
    - This preserves backward compatibility for any imports of the old ImageGen default export path while routing to the merged component.

    **Add translation keys to `client/src/locales/en/translation.json`:**
    - `"com_ui_generating_image": "Generating image..."` (for legacy path in-progress text)
    - `"com_ui_image_gen_finished": "Image created"` (alias -- or just reuse `com_ui_image_created`)
    - `"com_ui_image_gen_failed": "Image generation failed"` (domain-specific error text per user decision)
    - Add keys alphabetically near existing `com_ui_image_*` keys.
  </action>
  <verify>
    <automated>cd /home/berry13/librechat && npx tsc --noEmit --project client/tsconfig.json 2>&1 | head -30 && cd /home/berry13/librechat/client && npx jest --testPathPattern="__tests__/ImageGen" --no-coverage -x 2>&1 | tail -20</automated>
  </verify>
  <done>
    - OpenAIImageGen.tsx accepts both legacy props (initialProgress, args only) and agent-style props (full set)
    - Legacy path uses useProgress for standard progress; agent-style keeps quality-aware timing
    - ToolIcon(type="image_gen") renders for status indication with isAnimating
    - No inline SVGs, no hardcoded rgb colors, no ProgressCircle usage
    - ProgressText uses localized strings for all paths including legacy fallback
    - Domain-specific error text ("Image generation failed") not generic
    - Old ImageGen.tsx is a thin re-export to merged component
    - No collapsible panel, no subtitle
    - TypeScript compiles without errors
    - ImageGen tests from Wave 0 pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Update Part.tsx dispatch for merged ImageGen</name>
  <files>
    client/src/components/Chat/Messages/Content/Part.tsx
  </files>
  <action>
    **Update both image gen dispatch paths in Part.tsx:**

    1. **Agent-style path (line 131-141):** Change `<OpenAIImageGen ...>` to use the same component but ensure the import is correct. Currently Part.tsx imports `{ OpenAIImageGen }` from `'./Parts'`. After Task 1, `ImageGen` is also exported from Parts. Update the import to use `ImageGen` instead of `OpenAIImageGen`. The props stay the same: `initialProgress`, `isSubmitting`, `toolName`, `args`, `output`, `attachments`.

    2. **Legacy function-type path (line 192-193):** Change `<ImageGen initialProgress={progress} args={fn.arguments as string} />` to use the merged component. The merged component accepts optional `isSubmitting`, `toolName`, `output`, and `attachments`. Pass what is available from the legacy call context:
       - `initialProgress={progress}` (same as before)
       - `args={fn.arguments as string}` (same as before)
       - `isSubmitting={isSubmitting}` (available from Part props -- pass it so the component can detect streaming vs completed)
       - `toolName={fn.name}` (available -- the function name like 'dalle', 'flux', 'stable-diffusion')
       - Do NOT pass `output` or `attachments` for legacy path (not available in FunctionToolCall shape)

    3. **Update imports at top of Part.tsx:**
       - Remove: `import ImageGen from './ImageGen';` (old direct import)
       - The import `{ OpenAIImageGen, ... } from './Parts'` should be changed to `{ ImageGen, ... } from './Parts'` (or add ImageGen alongside OpenAIImageGen)
       - Both dispatch paths now use `<ImageGen ... />`

    4. **Verify ToolCallRenderers.ts is still correct:** Legacy ToolCallTypes.FUNCTION is already excluded from grouping since only ToolCallTypes.TOOL_CALL is groupable. No changes needed to ToolCallRenderers.ts, but verify by reading it. If it references OpenAIImageGen by name anywhere, update to ImageGen.
  </action>
  <verify>
    <automated>cd /home/berry13/librechat && npx tsc --noEmit --project client/tsconfig.json 2>&1 | head -30 && cd /home/berry13/librechat/client && npx jest --testPathPattern="__tests__/ImageGen" --no-coverage -x 2>&1 | tail -20</automated>
  </verify>
  <done>
    - Both image gen dispatch paths in Part.tsx render the same merged ImageGen component
    - Agent-style tools (image_gen_oai, image_edit_oai, gemini_image_gen) pass full props including isSubmitting, toolName, output, attachments
    - Legacy function-type tools (dalle, flux, stable-diffusion) pass initialProgress, args, isSubmitting, toolName
    - Old ImageGen import removed; using ImageGen from Parts barrel
    - No references to old OpenAIImageGen name remain in Part.tsx
    - TypeScript compiles without errors
    - ImageGen tests pass
    - ToolCallRenderers.ts verified unchanged (legacy FUNCTION type already excluded from grouping)
  </done>
</task>

</tasks>

<verification>
1. `cd /home/berry13/librechat && npx tsc --noEmit --project client/tsconfig.json` -- TypeScript compiles clean
2. `cd /home/berry13/librechat/client && npx jest --testPathPattern="__tests__/ImageGen" --no-coverage -x` -- ImageGen behavioral tests pass
3. `cd /home/berry13/librechat/client && npx jest --testPathPattern="Content/__tests__" --no-coverage -x` -- All Content tests pass
4. Grep for hardcoded strings: `grep -rn "Creating Image\|Finished\." client/src/components/Chat/Messages/Content/Parts/OpenAIImageGen/` should return zero matches
5. Grep for hardcoded colors: `grep -rn "rgb(" client/src/components/Chat/Messages/Content/Parts/OpenAIImageGen/` should return zero matches
6. Grep for ProgressCircle: `grep -rn "ProgressCircle" client/src/components/Chat/Messages/Content/Parts/OpenAIImageGen/` should return zero matches
</verification>

<success_criteria>
- Single ImageGen component handles both legacy and agent-style image gen tools
- No inline SVGs, no hardcoded rgb colors, no ProgressCircle in the merged component
- All status text localized via useLocalize
- Domain-specific wording ("Generating image...", "Image created", "Image generation failed")
- ToolIcon(type="image_gen") used for status icon with isAnimating animation
- PixelCard preserved for agent-style tools only
- Quality-aware timing preserved for OpenAI/Gemini tools; useProgress for legacy tools
- Both Part.tsx dispatch paths verified routing to same component
- TypeScript builds, ImageGen behavioral tests pass, existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-legacy-tool-migration/01-01-SUMMARY.md`
</output>
