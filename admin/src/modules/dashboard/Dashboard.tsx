function Dashboard() {
  return (
    <div>
      <h1>Dashboard</h1>
      <p>
        Some content for the main dashboard page will go here, such as showing total token usage for
        each model. Check if settings have been created in env or database if not, show a message
        here.
      </p>
    </div>
  );
}

export default Dashboard;
