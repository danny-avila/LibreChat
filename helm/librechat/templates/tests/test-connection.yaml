apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "brainiac.fullname" . }}-test-connection"
  labels:
    {{- include "brainiac.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "brainiac.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
