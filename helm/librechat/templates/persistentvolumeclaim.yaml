{{- if .Values.brainiac.imageVolume.enabled }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "brainiac.fullname" $ }}-images
spec:
  accessModes:
    - {{ .Values.brainiac.imageVolume.accessModes }}
{{- if .Values.brainiac.imageVolume.storageClassName }}
  storageClassName: {{ .Values.brainiac.imageVolume.storageClassName }}
{{- end }}          
  volumeMode: Filesystem
  resources:
    requests:
      storage: {{ .Values.brainiac.imageVolume.size }}
{{- end }}