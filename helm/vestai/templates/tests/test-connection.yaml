apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "vestai.fullname" . }}-test-connection"
  labels:
    {{- include "vestai.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "vestai.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
