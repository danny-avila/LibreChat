replicaCount: 1

secrets:
  storeRef: fake-secret-store
  key: librechat-test

config:
  env:
    # DO NOT USE redis it is not implemented with namespace prefix
    # and clashes with other services
    MEILI_HOST: "http://librechat-meilisearch:7700"
    MEILI_HTTP_ADDR: "librechat-meilisearch:7700"
    DOMAIN_CLIENT: "http://localhost"
    DOMAIN_SERVER: "http://localhost"

  file:
    enabled: true

  dotEnv:
    enabled: true

persistence:
  enabled: true
  storageClass: ""
  accessMode: ReadWriteOnce
  size: 8Gi

meilisearch:
  persistence:
    enabled: true
    storageClass: ""
    accessMode: ReadWriteOnce
    size: 8Gi
  auth:
    existingMasterKeySecret: "librechat-meili"

mongodb:
  enabled: true

  createSecret: false

  auth:
    usernames:
      - librechat
    databases:
      - librechat
    existingSecret: "librechat-mongodb"

  persistence:
    enabled: true
    storageClass: ""
    accessMode: ReadWriteOnce
    size: 8Gi
